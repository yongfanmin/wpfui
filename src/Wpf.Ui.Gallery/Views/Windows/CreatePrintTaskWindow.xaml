<ui:FluentWindow
    x:Class="Wpf.Ui.Gallery.Views.Windows.CreatePrintTaskWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wpf.Ui.Gallery.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:dialog="clr-namespace:Wpf.Ui.Gallery.Views.Windows"
    mc:Ignorable="d"
    Title="创建打印任务"
    MinHeight="250"
    MinWidth="600"
    ExtendsContentIntoTitleBar="True"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Title="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type local:CreatePrintTaskWindow}}}"
            Grid.Row="0"
            Icon="pack://application:,,,/Assets/applicationIcon-256.png"
        />
        <Grid x:Name="RootGrid" Grid.Row="1" Margin="20" />
        <Separator
            Grid.Column="3"
            Height="16"
            Margin="4,0,4,0" />
        <ui:Button
            Command="{Binding ViewModel.OpenPrintSettingsDialogCommand}"
            Icon="{ui:SymbolIcon Settings24}"
            ToolTip="印刷设置" />
    </Grid>
</ui:FluentWindow>