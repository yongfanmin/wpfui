<ui:FluentWindow
    x:Class="Wpf.Ui.Gallery.Views.Windows.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wpf.Ui.Gallery.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:Wpf.Ui.Gallery.ViewModels.Windows"
    xmlns:localConverters="clr-namespace:Wpf.Ui.Gallery.Converters"
    Title="Login"
    Width="400"
    Height="300"
    d:DataContext="{d:DesignInstance viewmodels:LoginWindowViewModel}"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <localConverters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    <Grid>
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Vertical">
            <StackPanel.Style>
                <Style TargetType="{x:Type StackPanel}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="IsEnabled" Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <ui:TextBlock
                Margin="0,0,0,12"
                HorizontalAlignment="Center"
                FontSize="24"
                FontWeight="Medium"
                Text="生产软件" />
            <ui:TextBox
                Width="200"
                Margin="0,6,0,0"
                PlaceholderText="账号"
                Text="{Binding Account, Mode=TwoWay}" >
                <ui:TextBox.Icon>
                    <ui:SymbolIcon Symbol="Person24" />
                </ui:TextBox.Icon>
            </ui:TextBox>
            <ui:PasswordBox
                Width="200"
                Margin="0,6,0,0"
                PlaceholderText="密码"
                Password="{Binding Password, Mode=TwoWay}" >
                <ui:PasswordBox.Icon>
                    <ui:SymbolIcon Symbol="LockClosed24" />
                </ui:PasswordBox.Icon>
            </ui:PasswordBox>
            <CheckBox
                Margin="0,12,0,0"
                Content="记住我 (下次无需密码登录)"
                IsChecked="{Binding RememberMe, Mode=TwoWay}" />
            <ui:Button
                Width="200"
                Margin="0,12,0,0"
                Appearance="Primary"
                Command="{Binding LoginCommand}"
                Content="登录" />
        </StackPanel>
        <ui:ProgressRing
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsIndeterminate="True"
            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</ui:FluentWindow>
